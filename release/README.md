# GitHub release package

Run `./release/build.ps1 -Zip` from PowerShell to refresh `release/tenno-link` and create the versioned ZIP for GitHub Releases. The `tenno-link` folder contains only the files Chrome needs; it is also usable with **Load unpacked**. Run the build after source changes and before tagging a release.
