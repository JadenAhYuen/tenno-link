# Contributing

Use feature branches and pull requests.

Keep extension permissions minimal, avoid adding remote executable code, and document any new external endpoint.

UI changes should support `prefers-reduced-motion`.
